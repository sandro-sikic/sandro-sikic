name: Build sqlit

on:
  schedule:
    - cron: '0 0 1 * *' # First day of every month
  workflow_dispatch: # Allow manual trigger
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: sqlit
  REGISTRY_USERNAME: ${{ github.actor }}
  REGISTRY_PASSWORD: ${{ secrets.GHCR }}

jobs:
  sqlit:
    runs-on: ubuntu-latest
    env:
      SERVICE_NAME: latest # Name of the service in the docker-compose file
      DOCKERFILE_LOCATION: configs/sqlit # Path to Dockerfile directory in the repository

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Docker login
        run: docker login --username ${{ env.REGISTRY_USERNAME }} --password ${{ env.REGISTRY_PASSWORD }} ${{ env.REGISTRY }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker buildx build ${{ env.DOCKERFILE_LOCATION }} \
            --tag ${{ env.REGISTRY }}/${{ env.REGISTRY_USERNAME }}/${{ env.IMAGE_NAME }}:${{ env.SERVICE_NAME }} \
            --cache-from type=registry,ref=${{ env.REGISTRY }}/${{ env.REGISTRY_USERNAME }}/${{ env.IMAGE_NAME }}:${{ env.SERVICE_NAME }}-cache \
            --cache-to type=registry,ref=${{ env.REGISTRY }}/${{ env.REGISTRY_USERNAME }}/${{ env.IMAGE_NAME }}:${{ env.SERVICE_NAME }}-cache,mode=max \
            --push
